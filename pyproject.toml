[project]
name = "paper-search"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "paper-search-scripts",
]

[tool.uv.sources]
paper-search-scripts = { workspace = true }

[tool.uv.workspace]
members = ["core", "backend", "scripts"]
